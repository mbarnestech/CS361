from pages import welcome_page


if __name__ == "__main__":
    """
    More needs to be added later to pull in the microservices, 
    but this is the main program. 
    Currently only the welcome page needs to be called.
    """
    welcome_page()
